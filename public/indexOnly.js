let typed = new Typed(".typed-title", {
  // Waits 1000ms after typing "First"
  strings: ["Decision should be easy", "An app that knows your interests, your dislikes, and allergens."],
  typeSpeed: 100,
  fadeOut: true,
  shuffle:true,
});
